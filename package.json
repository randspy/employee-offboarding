{
  "name": "employee-onboarding-portal",
  "version": "1.0.0",
  "description": "To run the app, you need to launch the servers in server and client directories.",
  "scripts": {
    "preinstall": "cd server && npm install && cd ../client && npm install",
    "start": "concurrently \"cd server && npm run dev\" \"cd client && npm run start\"",
    "test": "cd client && npm test"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "8.0.1"
  },
  "volta": {
    "node": "22.12.0"
  }
}
